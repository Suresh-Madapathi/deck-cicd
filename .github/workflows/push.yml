name: Run Deck
on: push
jobs:
 deck-sync:
    runs-on: ubuntu-latest
    steps:
      - name: Setup Deck
        uses: kong/setup-deck@v1
        with:
           deck-version: "1.8.0"
      - name: clone-repo
        uses: actions/checkout@v2
      - name: run deck synch
        run: deck gateway ping
